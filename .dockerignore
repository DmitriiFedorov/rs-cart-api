# Ignore node_modules since the Dockerfile has npm install inside it
node_modules

# .serverless is a leftover of task8 to deploy app in a Lambda
.serverless

# In the chore folder are placed a couple of sql files which are not needed to start the app
chore

.git